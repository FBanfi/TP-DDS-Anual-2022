package domain.organizaciones.cuenta;

public class HashingContrasenia {

    /*private static String formatoHash = "%02x"; // Hexa
    private static String algoritmo = "SHA";

    public static String doHashing (String password) throws NoSuchAlgorithmException {
      MessageDigest messageDigest = MessageDigest
          .getInstance(algoritmo); // Elige al algoritmo de hashing

      messageDigest.update(password.getBytes()); // Se podria agregar un Salt
      messageDigest.update(salt);

      byte[] resultByteArray = messageDigest.digest();

      StringBuilder sb = new StringBuilder();

      for (byte b : resultByteArray) { //Transforma los bytes del hash en string hexa
        sb.append(String.format(formatoHash, b));
      }

      return sb.toString();
}
    public byte[] crearSalt(){

    }
*/
}

